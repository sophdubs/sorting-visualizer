import React from 'react';

class Graph extends React.Component {
    render() {
        return (
            <div className="graph">
                I am a graph
            </div>
        );
    };
}

export default Graph;